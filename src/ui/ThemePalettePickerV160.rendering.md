# V1.6.0 palette picker rendered acceptance

The permanent visual gate runs `scripts/visual-theme-palette-picker.mjs` through `npm run visual:check`.

It verifies English/light and Arabic/dark fixtures, all ten palette options, persisted Light Blue selection, reset to Standard, appearance-mode independence, RTL direction and mobile horizontal-overflow containment.
