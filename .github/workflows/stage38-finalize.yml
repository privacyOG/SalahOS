name: Stage 38 finalize

on:
  push:
    branches:
      - 'stage-38/local-network-api'

permissions:
  contents: write

jobs:
  finalize:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: stage-38/local-network-api
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies from lockfile
        run: npm ci --ignore-scripts
      - name: Apply canonical formatting
        run: npm run format
      - name: Mark Stage 38 complete
        run: |
          python3 - <<'PY'
          from pathlib import Path
          path = Path('TODO.md')
          text = path.read_text()
          old = '- [ ] Optional local network API for smart-home/display integration'
          new = '- [x] Optional local network API for smart-home/display integration'
          if text.count(old) != 1:
              raise SystemExit(f'expected exactly one local-network API roadmap row, found {text.count(old)}')
          path.write_text(text.replace(old, new))
          PY
      - name: Full repository check
        run: npm run check
      - name: Commit verified Stage 38 result
        run: |
          rm .github/workflows/stage38-finalize.yml
          git config user.name privacyOG
          git config user.email kriptiik@proton.me
          git add -- .gitignore TODO.md docs/LOCAL_NETWORK_API.md eslint.config.js package.json local-api .github/workflows/stage38-finalize.yml
          git commit -m 'Finalize Stage 38 local network API'
          git push origin HEAD:stage-38/local-network-api
