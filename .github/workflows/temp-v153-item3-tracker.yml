name: Temporary v1.5.3 Item 3 Tracker Cleanup

on:
  push:
    branches: [v1.5.3-work-item-3]

permissions:
  contents: write

jobs:
  cleanup:
    if: contains(github.event.head_commit.message, '[item3-tracker-cleanup]')
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Delete temporary tracker tag
        run: git push origin :refs/tags/temp-v153-item3-tracker
