name: Temporary Prettier Autofix

on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/temporary-format-autofix.yml'

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Format repository
        run: npx prettier --write .

      - name: Remove temporary workflow from formatted branch
        run: rm .github/workflows/temporary-format-autofix.yml

      - name: Commit formatting branch
        run: |
          git config user.name "privacyOG"
          git config user.email "privacyOG@users.noreply.github.com"
          git add -A
          git commit -m "style: apply repository formatting"
          git push --force origin HEAD:v151-format-autofix
