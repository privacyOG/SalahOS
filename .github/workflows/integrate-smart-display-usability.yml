name: Integrate Smart Display Usability

on:
  push:
    branches:
      - agent/smart-display-tv-usability

permissions:
  contents: write

jobs:
  integrate:
    if: github.ref == 'refs/heads/agent/smart-display-tv-usability'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: agent/smart-display-tv-usability

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies from lockfile
        run: npm ci --ignore-scripts

      - name: Repository policies
        run: npm run security:sensitive-files && npm run security:audit && npm run security:licenses && npm run docs:links && python scripts/generate_pwa_icons.py --check

      - name: Apply usability integration
        run: python scripts/apply_smart_display_usability.py

      - name: Canonicalize changed sources
        run: npx prettier --write src/App.tsx src/smart-display.css src/platform/smartDisplayNavigation.ts src/platform/smartDisplayNavigation.test.ts scripts/smart-display-usability.test.mjs docs/TV_KIOSK_DEPLOYMENT.md

      - name: Validate implementation
        run: npm run lint && npm run typecheck && npm run test && npm run build && npm run verify:web-build

      - name: Commit integration
        run: |
          if git diff --quiet -- src/App.tsx src/smart-display.css; then
            echo "No integration changes required"
            exit 0
          fi
          git config user.name privacyOG
          git config user.email 102720550+privacyOG@users.noreply.github.com
          git add src/App.tsx src/smart-display.css
          git commit -m "feat: integrate smart display usability controls"
          git push origin HEAD:agent/smart-display-tv-usability
