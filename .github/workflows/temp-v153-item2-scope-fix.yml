name: Temporary v1.5.3 Item 2 Scope Fix

on:
  push:
    branches: [v1.5.3-work-item-2]

permissions:
  contents: write

jobs:
  regenerate:
    if: contains(github.event.head_commit.message, '[item2-scope-fix]')
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: v1.5.3-work-item-2
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci --ignore-scripts
      - name: Narrow address normalization and regenerate
        run: node scripts/temp-v153-item2-scope-fix.mjs
      - name: Format generated files
        run: npx prettier scripts/generate-australian-mosque-finder.mjs src/data/australian-mosque-finder.json src/data/australian-mosques-combined.json public/mosque-packs --write
      - name: Commit and publish temporary tag
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add scripts/generate-australian-mosque-finder.mjs src/data/australian-mosque-finder.json src/data/australian-mosques-combined.json public/mosque-packs
          git commit -m '[item2-scope-generated] narrow address normalization'
          git tag -f temp-v153-item2-scope-generated
          git push --force origin refs/tags/temp-v153-item2-scope-generated
